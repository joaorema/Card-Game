#ifndef MENU_HPP
# define MENU_HPP

#include <SFML/Graphics.hpp>
#include <tuple>

std::tuple<sf::Text, sf::Text, sf::Text, sf::Text, sf::Text>   //creates a way to handle all class Text in menu
start_menu(sf::Font& font);


#endif